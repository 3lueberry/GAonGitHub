import React, { useState } from "react";
// import styled from "styled-components";
import { v4 as uuidv4 } from "uuid";

const NewForm = ({ newExpense, setNewExpense, expenseList, setExpenseList, date }) => {
  return <div></div>;
};

export default NewForm;
